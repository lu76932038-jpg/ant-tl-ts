# 以下是询价中心待办处理任务要求

## 问题清单

[X]\[20260118-1\] ID0001:点击上传文件以后，确认提示以后，上传界面就关闭，然后，上传的任务就显示再列表中，但是，状态显示进行中，下载按钮显示生成中
[X]\[20260118-2\] ID0002:解析中的时候，要显示已经解析的时间，单位是秒，实时运转
[X]\[20260118-2\] ID0003:解析完成后，自动显示成功或者失败，失败的话，提示失败原因
[X]\[20260118-2\] ID0004:增加完成时间
[X]\[20260118-2\] ID0005:增加评价和留言功能，针对每一个任务
[X]\[20260118-2\] ID0006:表格根据 操作，状态，任务ID，文件名，文件类型，提交时间，完成时间，耗时，评价，留言 根据以上排序
[X]\[20260118-3\] ID0007:状态下面，解析中 ，没有显示已经进行了多少时间，而且我新上传的两个任务都一直再解析中，没有完成过
[X]\[20260118-3\] ID0008:提交时间和完成时间 显示 格式 YYYY-MM-DD HH:mm:ss
[X]\[20260118-3\] ID0009:已经完成的，耗时 = 完成时间 - 提交时间，如果解析失败，耗时 = 失败时间 - 提交时间，如果老数据，都显示0
[X]\[20260118-3\] ID0010:评价和留言功能 无法使用
[X]\[20260118-3\] ID0011:所有列名居中显示
[X]\[20260118-3\] ID0012:类型后面增加提交人
[X]\[20260118-3\] ID0013:只有已完成前的CHECKBOX才能沟选，其他都不能选，禁用状态
[X]\[20260119-2\] ID0014:操作增加终止按钮，在解析中的时候可用，点击终止，任务状态显示终止
[X]\[20260119-2\] ID0014:下载目前可以下载原图，可以解析后的文件，目前流程是，上传文件，解析，AI解析，我需要可以下载没有经过AI解析的Excel文件，之后下载可以分成三个文件，一个源文件，一个没有经过AI智能匹配的Excel文件，一个AI智能匹配后的Excel文件
[X]\[20260119-2\] ID0014-1:ID0014的任务感觉没有说明白，现在上传的资料，解析出来以后，可以解析出来，ABCD字段字段，根据我要求，匹配成 EFGH四个四段生成EXCEL表格，那么根据原生解析出来的ABCD字段，我们叫做没有经过智能匹配的数据，我需要它也能生成一个Excel下载，然后现在的是经过AI解析生成的EFGH文件也需要可以下载
[X]\[20260119-2\] ID0015:增加详情页面，详情页面显示解析的结果，包括清单（清单分成两种，1个是AI解析前的，1个是AI解析后的），解析过程
[X]\[20260119-2\] ID0016:管理员可以查看所有提交的任务
[X]\[20260119-2\] ID0017:任务执行完成后，解析应该会自动切换到已完成，现在需要刷新一下，才会变成已完成，失败同理
[X]\[20260119-2\] ID0018:增加一些基本检索功能
[X]\[20260119-2\] ID0019:标题显示 询价管理，菜单名也一起更新，其他英语和slogen都去掉
[X]\[20260119-2\] ID0020:除了显示总任务数，已完成，处理中，已终止和失败也需要显示，点击图标，可以显示对应的任务列表
[X]\[20260119-2\] ID0020: AI解析的表头如下，不允许变更列名和顺序
    询价类型,品牌,产品名称,询价型号,数量,物料单位,询价备注,是否带图纸,客户物料编码,目标价格,参考货期,期望交期(年-月-日),预计年用量,客户订单号,客户项目号
[X]\[20260119-2\] 解析匹配不上的，统一拼接到 询价备注
[X]\[20260120-1\] ID0021:菜单 询价任务中心 改成 询价管理作为二级菜单
[X]\[20260120-1\] ID0022:页面 询价管理 改成询价解析列表，作为 询价管理的三级菜单
[X]\[20260120-1\] ID0023:页面 任务详情，改成询价解析详情，作为 询价管理的三级菜单
[X]\[20260120-1\] ID0024:询价解析列表，增加时间维度的检索和筛选，检索和筛选排版重新设计，增加分页设计，去除详情按钮，直接点击任务ID可以进入详情页面，性情页面追加评论tab页面，可以记录每一次评论和评价
[X]\[20260120-1\] ID0025:询价解析列表,询价解析详情 页面排版以及页面背景重新用 nanobanana设计
[X]\[20260120-1\] ID0026:询价解析列表，文件类型用图标表示即可，不用显示中文
[X]\[20260120-2\] ID0027:图标里面 正在处理 里面 只有又任务在处理的时候，需要转动效果，没有正在处理的任务的时候，不需要转动
[X]\[20260120-2\] ID0028:菜单缩进按钮 无法使用
[X]\[20260120-2\] ID0028-1:缩进状态下，鼠标移动到图标上，图标不显示了
[X]\[20260120-2\] ID0029:询价解析列表的表头和表尾是否可以根据屏幕大小自适应，然后可以锁定，表头和表尾 看截图
[X]\[20260120-2\] ID0029-1:表头没有根据要求锁定，而且样式很奇怪，我的要求是我在移动滚动轴的时候，只有表格里面的内容需要上下滑动就可以
[X]\[20260120-2\] ID0029-2:表尾会上下自动移动，体验感不好
[X]\[20260120-2\] ID0030:日期搜索框，显示成 年-月-日 格式，点击后直接可以选择开始和结束日期
[X]\[20260120-3\] ID0031:询价解析列表类型值仅显示图标
[X]\[20260120-3\] ID0032:询价解析列表隐藏留言内容，仅保留评价图标显示
[X]\[20260120-2\] ID0034:询价解析列表，设计新手引导（页面遮罩+提示），给出设计思路，确认后执行
[X]\[20260120-2\] ID0035:询价管理下面增加三级菜单，操作指引，点击后显示操作指引页面，操作指引页面内容看截图,默认全部人员可以查看，给出操作手册的说明

### 2.0.6变更记录

[X]\[20260122-1\] 1 询价解析列表的帮助提示按钮，放到最右边
[X]\[20260122-1\] 2 帮助提示按钮，左边追加一句话，已经有 XXX个 用户，使用询价解析工具，成功解析XXX个询价单。不需要实时刷新
[X]\[20260122-1\] 3 合并导出按钮的导出格式要和AI解析后的格式一致

### 代办任务处理流程

1 需要你以产品经理的身份，处理虚假任务中心待办任务，评估需求的合理性，执行详细需求任务
2 需要你以资深开发工程师的身份，指定开发方案后执行  
3 根据顺序从上处理到最后，前面[X]的代表已经执行完成，不需要分析和执行
4 处理完成后，检查代码问题，有问题的独立修复
5 处理完成后，前面更新成 `[X]\[20260122-1\]`
6 所有执行过程和结果，都使用简体中文
